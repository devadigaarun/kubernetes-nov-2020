apiVersion: v1
kind: Pod
metadata:
  name: pod1

spec:
  containers:
    - name: c1
      image: nginx

## Deploy this pod
# $ kubectl apply -f pod1.yml
# $ kubectl get po
# $ kubectl delete -f pod1.yml